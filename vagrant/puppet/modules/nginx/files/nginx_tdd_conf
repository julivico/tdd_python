server {
	listen 80;
	
	
	location /static {
		alias /home/vagrant/sites/tdd.django.staging/static;
	}
	
	location / {
		proxy_set_header Host $host;
		proxy_pass http://unix:/tmp/tdd.django.staging.socket;
	}
}
